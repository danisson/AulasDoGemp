Seja uma função chamada "mdc" que leva dois inteiros num outro inteiro.

Seja os argumentos dessa função "a" e "b":
	Se "a" é menor que "b" então retorne "mdc b a"
	Se "b" é igual a zero então retorne "a"
	Senão retorne "mdc b (resto da divisão de a por b)"